# Disk-Usage-Analyzer-and-Cleaner
This project is a command-line based disk analysis and cleanup tool that: - Scans a given directory recursively - Calculates total disk usage and file count - Identifies the largest files in the directory - Detects common junk files  - Safely deletes junk files after user confirmation - Allows **careful, user-selected deletion of large files**
